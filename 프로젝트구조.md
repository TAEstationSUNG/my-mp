# 📁 프로젝트 구조 안내 (내 의원 뭐하나)

> `src` 폴더는 없어요. Next.js는 `app/` 폴더를 프로젝트 루트에 두는 방식도 표준이라, 우리는 그 방식이에요.

## 전체 구조

```
my-funny/
├── app/                    ← 🖥️ 화면(웹페이지). "보이는 것"
│   ├── layout.js              헤더 + 공통 뼈대 (로고 "내 의원 뭐하나")
│   ├── page.js                홈 화면 (지역 검색 + 의원 목록)
│   ├── globals.css            전체 디자인/색상 (컬러풀·라이트/다크)
│   └── member/[monaCd]/
│       └── page.js            의원 프로필 화면
│
├── lib/                    ← 🔧 공용 도구
│   ├── supabase.js            DB 접속 설정
│   └── ui.js                  정당색·아바타 헬퍼
│
├── scripts/                ← 📥 데이터 수집 (화면 아님, 터미널 실행)
│   ├── ingest.mjs             API → Supabase 적재 (npm run ingest:*)
│   └── test-assembly-api.mjs  API 검증용
│
├── supabase/
│   └── schema.sql             DB 테이블 설계도
│
├── .env                    ← 🔑 비밀 키 (깃에 안 올라감)
├── .env.example               .env 견본 (값 없는 양식)
├── package.json               프로젝트 설정 + 명령어 목록
├── next.config.mjs            Next.js 설정
├── README.md                  프로젝트 설명 + Phase 로드맵
└── 프로젝트구조.md            (이 파일)
```

## "무엇을 어디서 보나"

| 보고 싶은 것 | 어디 |
|---|---|
| 실제 웹사이트 화면 | 브라우저 http://localhost:3000 (`npm run dev` 켠 상태) |
| 홈 화면 코드 | `app/page.js` |
| 의원 프로필 코드 | `app/member/[monaCd]/page.js` |
| 색깔·디자인 | `app/globals.css` |
| DB에 든 실제 데이터 | Supabase 대시보드 → Table Editor |
| 데이터 다시 수집 | 터미널 `npm run ingest:*` (코드는 `scripts/ingest.mjs`) |

## 핵심 구분

- **`app/`** = 눈에 보이는 웹페이지 (브라우저에서 보는 것)
- **`scripts/`** = 뒤에서 데이터 긁어오는 도구 (터미널 전용, 화면 아님)
- **`.next/`, `node_modules/`** = 자동 생성/설치된 것 → **건드릴 필요 없음** (탐색기에 보여도 무시)

## 자주 쓰는 명령어

```bash
npm run dev            # 웹사이트 개발 서버 켜기 (localhost:3000)
npm run ingest:members # 의원 299명 적재
npm run ingest:bills   # 발의법안 적재
npm run ingest:votes   # 의원 개인별 표결 적재 (오래 걸림)
npm run ingest:tags    # 이슈 태그 규칙 적용
npm run ingest:all     # 위 전부 한 번에
```

## 데이터 흐름 (한눈에)

```
열린국회정보 Open API
      │  (scripts/ingest.mjs 가 긁어옴)
      ▼
   Supabase (DB, supabase/schema.sql 로 만든 테이블)
      │  (app/ 의 화면이 읽어옴)
      ▼
   웹사이트 화면 (app/page.js, member/[monaCd]/page.js)
```
